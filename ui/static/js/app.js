import { api } from './api-client.js';
import { getState, setState } from './state-store.js';
import { monitorRun } from './polling.js';
import { clearNotice, notify } from './notifications.js';
import { renderFailures, renderResults } from './views/results.js';

const $ = selector => document.querySelector(selector);
const $$ = selector => [...document.querySelectorAll(selector)];
const views = $$('[data-view]');

function showView(view) { setState({view}); views.forEach(item => { item.hidden = item.dataset.view !== view; }); $$('.nav-item').forEach(item => item.classList.toggle('is-active', item.dataset.viewLink === view)); history.replaceState(null, '', `#${view}`); }
function selectedEngines(target) { return $$(`#${target} input:checked`).map(input => input.value); }
function renderEngines(target, engines, selected = []) { const root = $(`#${target}`); root.replaceChildren(); engines.forEach(engine => { const label = document.createElement('label'); label.className = 'engine-option'; const input = document.createElement('input'); input.type = 'checkbox'; input.value = engine.engine_id; input.checked = selected.includes(engine.engine_id); input.disabled = engine.readiness && engine.readiness !== 'enabled'; label.append(input, document.createTextNode(` ${engine.display_name || engine.engine_id}`)); root.append(label); }); }
function formatNumber(value) { return new Intl.NumberFormat().format(Number(value || 0)); }
function statusBadge(status) { const badge = document.createElement('span'); badge.className = `status-badge ${status}`; badge.textContent = status; return badge; }

async function loadConfiguration() { const configuration = await api.configuration(); const engines = await api.engines(); setState({configuration}); renderEngines('search-engines', engines.engines, configuration.configuration.search_engines); renderEngines('config-engines', engines.engines, configuration.configuration.search_engines); $('#country').value = configuration.configuration.country_code; $('#config-country').value = configuration.configuration.country_code; $('#search-type').value = configuration.configuration.search_type; $('#config-search-type').value = configuration.configuration.search_type; $('#pages').value = configuration.configuration.num_pages_for_keyword; $('#config-pages').value = configuration.configuration.num_pages_for_keyword; }
function renderMetrics(analytics) { $$('#overview-metrics [data-metric], #history-metrics [data-analytics]').forEach(element => { const key = element.dataset.metric || element.dataset.analytics; element.textContent = formatNumber(analytics[key]); }); }
function renderRecent(runs) { const root = $('#recent-runs'); root.replaceChildren(); if (!runs.length) { root.textContent = 'No searches have been recorded yet.'; return; } runs.slice(0, 5).forEach(run => { const item = document.createElement('a'); item.href = '#history'; item.className = 'run-item'; item.dataset.viewLink = 'history'; const content = document.createElement('span'); const title = document.createElement('strong'); title.textContent = run.query; const date = document.createElement('small'); date.textContent = new Date(run.created_at).toLocaleString(); content.append(title, date); item.append(content, statusBadge(run.status)); item.addEventListener('click', () => selectHistorical(run.id)); root.append(item); }); }
async function refreshOverview() { const [analytics, history] = await Promise.all([api.analytics(), api.history()]); setState({analytics, history: history.searches}); renderMetrics(analytics); renderRecent(history.searches); }
async function refreshHistory() { const query = $('#history-filter').value.trim(); const [analytics, history] = await Promise.all([api.analytics(), api.history(query)]); setState({analytics, history: history.searches}); renderMetrics(analytics); const root = $('#runs'); root.replaceChildren(); if (!history.searches.length) { const row = document.createElement('tr'); const empty = document.createElement('td'); empty.colSpan = 6; empty.className = 'empty-cell'; empty.textContent = 'No matching searches.'; row.append(empty); root.append(row); return; } history.searches.forEach(run => { const row = document.createElement('tr'); row.append(cell(new Date(run.created_at).toLocaleString()), cell(run.query)); const status = document.createElement('td'); status.append(statusBadge(run.status)); row.append(status, cell(formatNumber(run.result_count)), cell(formatNumber(run.failure_count))); const action = document.createElement('td'); const button = document.createElement('button'); button.className = 'button button-ghost'; button.type = 'button'; button.textContent = 'Inspect'; button.addEventListener('click', () => selectHistorical(run.id)); action.append(button); row.append(action); root.append(row); }); }
function cell(value) { const td = document.createElement('td'); td.textContent = value ?? ''; return td; }
async function selectHistorical(id) { setState({historicalRunId: id}); const run = getState().history.find(item => item.id === id); $('#historical-detail').hidden = false; $('#historical-label').textContent = run?.query || `Run ${id}`; const [results, failures] = await Promise.all([api.results(id), api.failures(id)]); renderResults($('#historical-results'), results.results); renderFailures($('#historical-failures'), failures.failures); showView('history'); }
function updateProgress(status) { const progress = status.progress || {}; const total = Number(progress.total_jobs || 0); const completed = Number(progress.completed_jobs || 0); $('#progress-card').hidden = false; $('#search-progress').value = total ? Math.round(completed / total * 100) : 0; $('#progress-status').textContent = `${status.status} · ${completed}/${total || '?'} jobs${progress.engine ? ` · ${progress.engine}` : ''}`; $('#progress-eta').textContent = ['completed', 'failed'].includes(status.status) ? 'Finished' : 'Updating'; $('#result-summary').textContent = `${formatNumber(status.result_count)} results · ${formatNumber(status.failure_count)} failures`; }
async function refreshCurrent() { const id = getState().activeRunId; if (!id) return; const [results, failures] = await Promise.all([api.results(id), api.failures(id)]); renderResults($('#results'), results.results); renderFailures($('#current-failures'), failures.failures); }
async function startSearch(event) { event.preventDefault(); clearNotice(); const query = $('#query').value.trim(); if (!query) return; try { const data = await api.send('/searches', 'POST', {query, options: {search_engines: selectedEngines('search-engines'), country_code: $('#country').value.toUpperCase(), search_type: $('#search-type').value, num_pages_for_keyword: Number($('#pages').value)}}); setState({activeRunId: data.id}); showView('search'); $('#current-label').textContent = query; $('#delete-current').disabled = false; await monitorRun({id: data.id, readStatus: api.status, onUpdate: async status => { updateProgress(status); await refreshCurrent(); }, onFinish: async () => { await refreshOverview(); await refreshHistory(); }}); } catch (error) { notify(error.message, 'error'); } }
async function deleteRun(id, all = false) { if (!id && !all) return; if (!window.confirm(all ? 'Delete the complete search archive?' : 'Delete this search and all results?')) return; try { await api.send(all ? '/history/searches' : `/searches/${encodeURIComponent(id)}`, 'DELETE'); setState({activeRunId: all ? null : getState().activeRunId, historicalRunId: null}); $('#historical-detail').hidden = true; await refreshOverview(); await refreshHistory(); notify(all ? 'Archive deleted.' : 'Search deleted.'); } catch (error) { notify(error.message, 'error'); } }
async function saveConfiguration(event) { event.preventDefault(); try { const data = await api.send('/configuration', 'PUT', {search_engines: selectedEngines('config-engines'), country_code: $('#config-country').value.toUpperCase(), search_type: $('#config-search-type').value, num_pages_for_keyword: Number($('#config-pages').value)}); setState({configuration: data}); await loadConfiguration(); notify(`Configuration revision ${data.revision} saved.`); } catch (error) { notify(error.message, 'error'); } }
async function init() { $$('.nav-item, [data-view-link]').forEach(link => link.addEventListener('click', event => { const view = link.dataset.viewLink; if (view) { event.preventDefault(); showView(view); if (view === 'history') refreshHistory().catch(error => notify(error.message, 'error')); } })); $('#search-form').addEventListener('submit', startSearch); $('#refresh-all').addEventListener('click', () => Promise.all([refreshOverview(), refreshHistory()]).catch(error => notify(error.message, 'error'))); $('#refresh-current').addEventListener('click', () => refreshCurrent().catch(error => notify(error.message, 'error'))); $('#history-filter').addEventListener('input', () => refreshHistory().catch(error => notify(error.message, 'error'))); $('#delete-current').addEventListener('click', () => deleteRun(getState().activeRunId)); $('#delete-historical').addEventListener('click', () => deleteRun(getState().historicalRunId)); $('#delete-all').addEventListener('click', () => deleteRun(null, true)); $('#config-form').addEventListener('submit', saveConfiguration); $('#reset-config').addEventListener('click', async () => { try { await api.send('/configuration/reset', 'POST'); await loadConfiguration(); notify('Defaults restored.'); } catch (error) { notify(error.message, 'error'); } }); await loadConfiguration(); await refreshOverview(); const initial = location.hash.slice(1); if (['overview', 'search', 'history', 'configuration'].includes(initial)) showView(initial); }
init().catch(error => notify(error.message, 'error'));
